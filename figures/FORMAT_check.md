# FORMAT_check.md — Journal of Chemometrics (Wiley) final-art compliance

Generated by `script_12_gp_and_consolidation.py` (addendum Task 8). Dimensions read back from the actual saved TIFF files (Pillow), not assumed from the matplotlib figsize.

Requirements checked: TIFF (LZW) at 600 dpi; width 80-180 mm; max reproduction 140x200 mm; IUPAC italic symbols with roman units; legend/key inside the figure (not caption-only).

| Figure | Width (mm) | Height (mm) | DPI | ≤180×200 mm? | Italic symbols used | Legend inside? |
|---|---|---|---|---|---|---|
| `fig_1.tiff` | 142.2 | 108.2 | 600 | Yes | k_ext, k_deg, sigma, T, R^2 | Yes |
| `fig_2_eda_pairplot.tiff` | 174.9 | 140.2 | 600 | Yes | T, t, C | Yes |
| `fig_3_phenomenological_fits.tiff` | 180.0 | 136.9 | 600 | Yes | t, C | Yes |
| `fig_4_kext_confidence_intervals.tiff` | 177.7 | 141.5 | 600 | Yes | k_ext | Yes |
| `fig_5_response_surfaces.tiff` | 180.0 | 108.9 | 600 | Yes | T, k_ext | Yes |
| `fig_6_out_of_condition_validation.tiff` | 173.5 | 110.9 | 600 | Yes | R^2 | Yes |
| `fig_7_augmentation_and_sigma.tiff` | 174.0 | 75.0 | 600 | Yes | R^2, sigma | Yes |
| `fig_8_gp_uncertainty_map.tiff` | 180.0 | 79.6 | 600 | Yes | T, k_ext | Yes |

## Notes
- All 7 figures (fig_2 ... fig_8) are exported as TIFF (LZW-compressed, lossless) at 600 dpi for submission, plus a matching PNG at 300 dpi (same base filename) for internal preview/working use.
- Physical-quantity symbols (*T*, *t*, *C*, *k*_ext, *R*², *σ*) are set in italics via matplotlib mathtext; descriptive subscripts (`ext`) and units (min⁻¹, °C) are set upright via `\mathrm{}` or plain text, per IUPAC convention.
- All legends/color keys are rendered inside the figure axes (`ax.legend()` / colorbar), not described only in the caption text.
- **Tables are NOT exported as figures.** Per Journal of Chemometrics requirements, tables go into the manuscript as native editable text (Word/LaTeX), sourced from `results/paper_table_1..5*.csv` and `paper_table_supp_nofilter.csv` — see `results/README_tables.md`.
- No analysis, data, or numeric result was changed by this formatting pass (`random_state=42` and all prior outputs are untouched).